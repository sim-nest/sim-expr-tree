[package]
name = "sim-lib-expr-tree-server"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
publish = false
description = "Scaffold for the SIM expression-tree server library."

[dependencies]
sim-lib-expr-tree = { path = "../sim-lib-expr-tree" }
sim-lib-view-expr-tree = { path = "../sim-lib-view-expr-tree" }

[lints]
workspace = true
