[package]
name = "sim-expr-tree-calc"
version = "0.1.0"
edition.workspace = true
license.workspace = true
readme = "README.md"
repository.workspace = true
homepage.workspace = true
publish = false
description = "Scaffold for SIM expression-tree calculation."

[dependencies]
sim-incremental-core = { version = "0.1.0" }
sim-expr-tree-core = { path = "../sim-expr-tree-core" }
sim-kernel = { version = "0.1.2" }
sim-table-core = { version = "0.1.2" }

[lints]
workspace = true
