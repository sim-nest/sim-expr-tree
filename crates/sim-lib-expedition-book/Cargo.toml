[package]
name = "sim-lib-expedition-book"
version = "0.1.1"
edition.workspace = true
license.workspace = true
readme = "README.md"
repository.workspace = true
homepage.workspace = true
publish = true
description = "Editable semantic expedition books over immutable SIM evidence references."

[dependencies]
sim-citizen = { version = "0.4.0" }
sim-citizen-derive = { version = "0.4.0" }
sim-kernel = { version = "0.4.0" }

[lints]
workspace = true
