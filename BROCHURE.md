# sim-expr-tree

`sim-expr-tree` is the planned SIM expression-tree workspace. The repository
currently contains only the public crate skeleton, validation gates, docs lanes,
and recipe scaffolding needed before implementation phases add checked behavior.
