[package]
name = "sim-lib-expr-tree-serve"
version = "0.1.1"
edition.workspace = true
license.workspace = true
readme = "README.md"
repository.workspace = true
homepage.workspace = true
publish = true
description = "Bootloader-loaded product recipe for SIM expression-tree sessions."

[dependencies]
sim-codec-lisp = { version = "0.2.1" }
sim-config = { version = "0.1.2" }
sim-kernel = { version = "0.2.0" }
sim-lib-expr-tree = { version = "0.2.0", path = "../sim-lib-expr-tree" }
sim-lib-expr-tree-server = { version = "0.2.0", path = "../sim-lib-expr-tree-server" }
sim-lib-server = { version = "0.1.11" }
sim-run-core = { version = "0.3.0" }
sim-web-shell = { version = "0.1.12" }

[dev-dependencies]
sim-kernel = { version = "0.2.0", features = ["test-support"] }

[lints]
workspace = true
