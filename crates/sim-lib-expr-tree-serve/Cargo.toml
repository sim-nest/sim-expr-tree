[package]
name = "sim-lib-expr-tree-serve"
version = "0.1.0"
edition.workspace = true
license.workspace = true
readme = "README.md"
repository.workspace = true
homepage.workspace = true
publish = true
description = "Bootloader-loaded product recipe for SIM expression-tree sessions."

[dependencies]
sim-codec-lisp = { version = "0.1.2" }
sim-config = { version = "0.1.2" }
sim-kernel = { version = "0.1.5" }
sim-lib-expr-tree = { version = "0.1.0", path = "../sim-lib-expr-tree" }
sim-lib-expr-tree-server = { version = "0.1.0", path = "../sim-lib-expr-tree-server" }
sim-lib-server = { version = "0.1.10" }
sim-run-core = { version = "0.2.0" }
sim-web-shell = { version = "0.1.11" }

[dev-dependencies]
sim-kernel = { version = "0.1.5", features = ["test-support"] }

[lints]
workspace = true
