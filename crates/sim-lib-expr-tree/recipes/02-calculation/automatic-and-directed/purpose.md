# Automatic and directed calculation

This recipe lets source mutation run bounded automatic work, switches one cell
to manual policy, and explicitly calculates it. It then inspects the receipt
from a deterministic cycle, repairs the source, recursively recalculates, and
opens the same storage name again to prove that source, policy, results, and
receipts remain available.
